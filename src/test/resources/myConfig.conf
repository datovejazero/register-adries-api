db = {
  url = "${JDBC_URL}"
  driver = "com.microsoft.sqlserver.jdbc.SQLServerDriver"
  connectionPool = "HikariCP"
  maximumPoolSize = 26
  numThreads = 5
  connectionTimeout = 30000
}

app {
  host = "localhost"
  port = 18081
  api-version = "v1"
}
